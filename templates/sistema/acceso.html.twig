{% extends 'base.html.twig' %}

{% block title %}Intranet - Sistemas{% endblock %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
{% block icon %}
    {{ parent() }}
{% endblock %}
{% block body %}
    {{ parent() }}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{#<a class="btn btn-info" style="margin:10px;" href="{{ path("app_logout") }}">Cerrar sesión</a>#}

{# Recorrido de los sistemas del usuario, recibido por parametro #}

    <div class="row" style="margin:auto;">
        <div class="col-6">
            <h3 style="text-align:center;">Sistemas habilitados</h3>
        </div>
        <div class="col-6">
            <h3 style="text-align:center;">Novedades</h3>
        </div>
    </div>

    <div class="row" style="margin:auto;padding-left:40px;">
        <div class="container-fluid cuadroNovedades col-6">
            
                {% for sistema in sistemas %}
                        <div class="container">
                            <div class="card cuadroSistema">
                              <div class="row ">
                                <div class="col-md-6">
                                    <div class="card-img-bottom">
                                        <img class="card-img-top" src="{{asset(('uploads/'~ sistema.logo))}}" alt="Card image cap">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                  <div class="card-block">
                                    <h4 class="card-title">{{ sistema.nombre }}</h4>
                                    <p class="card-text">{{ sistema.descripcion }}</p>
                                    <div class="btn-group">
                                            <a class="btn btn-outline-success" href="{{sistema.url}}">Conectar</a>
                                        
                                        
                                            <a class="btn btn-outline-primary" href="{{sistema.url}}"><img class="card-img-top" style="width:25px;" src="{{ asset("img/editar.png") }}" alt="Card image cap"></a>
                                        
                                        
                                            <a class="btn btn-outline-primary" href="{{sistema.url}}"><img class="card-img-top" style="width:25px;" src="{{ asset("img/eliminar.png") }}" width="10"alt="Card image cap"></a>
                                        
                                    </div>
                                  </div>
                                </div>
                                
                              </div>
                            </div>
                          </div>
                    
                {% endfor %}
            
        </div>
        
        
        <div class="cuadroNovedades col-6">
            KASLÑDKSALÑDJSAKDLÑSAJDKLSAJDKLSADJAS
            DSADJKSLADJSAKLDAS
            DSAJDKLSADJSAKL
        </div>
    </div>
    <h5 style="margin-left:15px;color:grey;">Los permisos se solicitan a <b>soporte@unraf.edu.ar</b></h5>
    <div class="container">
        {#<a class="btn btn-primary" href="{{ path("app_logout") }}">Volver</a>#}
    </div>
    {% block notificaciones %}
        {{ parent() }}
    {% endblock notificaciones %}

{% endblock %}
