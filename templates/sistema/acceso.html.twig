{% extends 'base.html.twig' %}

{% block title %}Intranet - Sistemas{% endblock %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
{% block icon %}
    {{ parent() }}
{% endblock %}
{% block body %}
    {{ parent() }}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{#<a class="btn btn-info" style="margin:10px;" href="{{ path("app_logout") }}">Cerrar sesión</a>#}

{# Recorrido de los sistemas del usuario, recibido por parametro #}

    <div class="row" style="margin:auto;">
        <div class="col-6">
            <h3 style="text-align:center;">Sistemas habilitados</h3>
        </div>
        <div class="col-6">
            <h3 style="text-align:center;">Novedades</h3>
        </div>
    </div>

    <div class="row" style="margin:auto;padding-left:40px;">
        <div class="container-fluid cuadroNovedades col-6">
            <div  class="row">
                {% for sistema in sistemas %}
                    <div  class="container-fluid col-{{12/tamanio}}">
                        <div class="container-fluid cuadroSistema">
                            <a href="{{sistema.url}}"><img class="imagenSistema" alt="logoSistema" src="{{asset(('uploads/'~ sistema.logo))}}" alt="Logo"></a>
                            <a class="nombreSistema" href="{{sistema.url}}"><h4>{{ sistema.nombre }}</h4></a>
                            <h6>{{ sistema.descripcion }}</h6>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="cuadroNovedades col-6">
            KASLÑDKSALÑDJSAKDLÑSAJDKLSAJDKLSADJAS
            DSADJKSLADJSAKLDAS
            DSAJDKLSADJSAKL
        </div>
    </div>
    <h5 style="margin-left:15px;color:grey;">Los permisos se solicitan a <b>soporte@unraf.edu.ar</b></h5>
    <div class="container">
        {#<a class="btn btn-primary" href="{{ path("app_logout") }}">Volver</a>#}
    </div>
    {% block notificaciones %}
        {{ parent() }}
    {% endblock notificaciones %}

{% endblock %}
